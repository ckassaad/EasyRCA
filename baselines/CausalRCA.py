import networkx as nx
import numpy as np
import pandas as pd


def learn_linear_scm(window_graph, data):
    for node_t in window_graph.nodes:
        1


def causalrca(window_graph, data, scm):
    if scm is None:
        scm = learn_linear_scm(window_graph, data)

    rc_list = []


if __name__ == '__main__':
    1